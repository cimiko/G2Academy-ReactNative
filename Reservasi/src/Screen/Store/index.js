import React from 'react'
import { SafeAreaView, ScrollView, View } from 'react-native'

function Store() {
    return (
        <View>
            <SafeAreaView>
                <ScrollView>
                    
                </ScrollView>
            </SafeAreaView>
        </View>
    )
}

export default Store

